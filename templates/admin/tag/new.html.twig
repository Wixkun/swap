{% extends 'base.html.twig' %}

{% block title %}Créer un Nouveau Tag{% endblock %}

{% block body %}
    <div class="container mx-auto px-4 py-8">
        <h1 class="text-3xl font-bold mb-6 text-gray-800">Créer un Nouveau Tag</h1>

        <div class="bg-white shadow rounded-lg p-6">
            {{ include('admin/tag/_form.html.twig', { 'class': 'space-y-4' }) }}
        </div>

        <div class="flex justify-between mt-6">
            <a href="{{ path('app_tag_index') }}" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded">
                ⬅ Retour à la liste
            </a>
        </div>
    </div>
{% endblock %}
