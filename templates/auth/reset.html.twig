<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Réinitialiser le mot de passe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans leading-normal tracking-normal">
<div class="min-h-screen flex items-center justify-center">
    <main class="w-full max-w-md mx-auto p-6">
        <header class="text-center mb-6">
            <h1 class="text-2xl font-bold text-black">
                Réinitialiser votre mot de passe
            </h1>
        </header>

        <section class="mb-4">
            {% for message in app.flashes('success') %}
                <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-2">
                    {{ message }}
                </div>
            {% endfor %}
            {% for message in app.flashes('error') %}
                <div class="bg-gray-100 border border-black text-black px-4 py-3 rounded relative mb-2">
                    {{ message }}
                </div>
            {% endfor %}
        </section>

        <div class="bg-white p-8 rounded-lg shadow">
            <form method="POST" action="{{ path('page_reset_password', {token: token}) }}" class="space-y-4">
                <div>
                    <label for="password" class="block text-gray-700 font-bold mb-2">Nouveau mot de passe</label>
                    <input
                        type="password"
                        id="password"
                        name="password"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-black"
                    />
                </div>
                <div>
                    <label for="repeat_password" class="block text-gray-700 font-bold mb-2">Répétez le mot de passe</label>
                    <input
                        type="password"
                        id="repeat_password"
                        name="repeat_password"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-black"
                    />
                </div>
                <button
                    type="submit"
                    class="w-full bg-black text-white py-2 rounded-lg hover:bg-black transition"
                >
                    Valider
                </button>
            </form>
        </div>
    </main>
</div>
</body>
</html>
