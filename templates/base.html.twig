<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Swap{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
</head>
<body class="bg-gray-100 text-gray-900">

<header class="bg-blue-600 text-white py-4">
    <div class="container mx-auto flex justify-between items-center px-4">
        <img src="{{ asset('images/logo.svg') }}" alt="Logo" class="h-12">
        <nav>
            <a href="{{ path('app_default') }}" class="text-lg font-semibold hover:underline">Accueil</a>
        </nav>
    </div>
</header>

<main class="container mx-auto mt-8 px-4">
    {% block body %}{% endblock %}
</main>

<footer class="bg-blue-600 text-white text-center py-4 mt-8">
    <p>&copy; 2024 Swap - Tous droits réservés</p>
</footer>

<script src="{{ asset('app.js') }}"></script>
</body>
</html>
